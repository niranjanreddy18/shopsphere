"""
Models for the analytics domain.

Intentionally empty — see apps.py's docstring. Every analytics endpoint is
a read-only aggregation over Order/OrderItem/Product/User, computed on
request rather than stored.
"""
